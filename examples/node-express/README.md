# Example Placeholder

This directory is reserved for a future generic Node/Express example implementation of the reverse-proxy-aware app-layer ban pattern.

It is intentionally empty for now because the current repository scope is documentation-first Part 1, without shipping instance-specific code.
